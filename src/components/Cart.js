import React, { useEffect } from 'react';

class Cart extends Component {

    // create variable to store the product's ID
    const productId = props.match.params.id;    

    // display cart 


    useEffect(() => {
        // if productId exists
        if(productId){
            dispatch
        }
    }
}

export default Cart;